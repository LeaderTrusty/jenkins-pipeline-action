name: Jenkins Pipeline

on:
  push:

jobs:
  jenkins:
    runs-on: ubuntu-latest

    container:
      image: jenkins/jenkins:lts

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run Jenkinsfile
        run: |
          echo "Simulating Jenkins pipeline"
          echo "Build stage"
          echo "Test stage"
          echo "Deploy stage"
